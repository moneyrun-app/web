# 프론트엔드 변경사항 — 2026-04-16

> v3.0 코스 시스템 후속 개선 + 버그 수정

---

## 1. 코스 완료 플로우 구현

**파일:** `src/app/(app)/course/missions/page.tsx`

- 모든 미션 완료 시 하단에 **"코스 완료하기"** 버튼 표시 (AnimatePresence)
- `POST /course/active/complete` 호출 후 축하 화면 전환
- 축하 화면: Trophy 아이콘 + "축하합니다!" + 완료 미션 수/수강 기간 요약
- **"다음 코스 선택하기"** 버튼 하나 → `/course/select` 이동

---

## 2. 페이스메이커 코스 미선택 유저 분기

**파일:** `src/app/(app)/pacemaker/page.tsx`

- `activeCourse` 없을 때 → 퀴즈 대신 **코스 선택 유도 전체화면** 표시
- 마이북 생성 중일 때 → 코스 선택 유도 대신 **생성 프로그레스 전체화면** 표시
- 코스 진도 바 위치: 마이북 생성 중이면 **프로그레스 카드**로 대체
  - 서버 progress (step, percent, chaptersDone/totalChapters) 표시
  - 생성 완료 시 자동으로 코스 진도 바로 전환 (쿼리 invalidate)

---

## 3. 코스 선택 페이지 수정

**파일:** `src/app/(app)/course/select/page.tsx`

- **백엔드 응답 타입 수정**: `AvailableCoursesResponse` → `AvailableCourse[]` (백엔드가 배열 직접 반환)
- 코스 시작 후 **생성 로딩 화면** 추가
  - `GET /course/generate/status/:purchaseId` 10초 간격 폴링
  - 서버 progress (step 메시지 + percent + 챕터 카운터) 표시
  - 완료 시 `/pacemaker` 이동, 실패 시 목록 복귀

---

## 4. 마이북 페이지 개선

**파일:** `src/app/(app)/my-book/page.tsx`

- **코스 마이북 프로그레스 바 카드 삭제** (페이스메이커에서 이미 코스 진도 확인 가능)
- **마이북 생성 중 프로그레스 카드** 추가: 탭 위에 생성 중인 책의 진행률 표시
- **완료된 코스 책에 "완료" 뱃지** 표시 (source=course, status=completed, 현재 활성 코스 아닌 것)
- 생성 완료 시 자동 새로고침 (`my-book-overview`, `active-course` invalidate)

---

## 5. 온보딩 수정

### StepCategorySelect (`src/components/onboarding/StepCategorySelect.tsx`)
- `onComplete` 콜백에 선택한 카테고리 전달 → 오케스트레이터에서 `store.setCategory()` 즉시 호출

### StepDiagnosticQuiz (`src/components/onboarding/StepDiagnosticQuiz.tsx`)
- `useOnboardingStep2Questions(enabled)`: 스토어의 `selectedCategory` 존재 여부로 enabled 제어

### StepFinanceInput (`src/components/onboarding/StepFinanceInput.tsx`)
- `onComplete` 콜백에 닉네임 전달 → 오케스트레이터에서 `userStore.setUser({ nickname })` 즉시 호출

### StepBookGeneration (`src/components/onboarding/StepBookGeneration.tsx`)
- 서버 `progress` 응답이 있으면 실제 진행률 표시 (percent + step 메시지 + 챕터 카운터)
- 서버 progress 없으면 기존 시간 기반 fallback 유지
- 로딩 메시지에 닉네임 + `님의` 붙여서 자연스럽게 표시

### 온보딩 오케스트레이터 (`src/app/(onboarding)/onboarding/page.tsx`)
- step1 완료 시 `store.setCategory(category)` 즉시 세팅
- step3 완료 시 `useUserStore.getState().setUser({ nickname })` 즉시 세팅

---

## 6. API 훅 변경

**파일:** `src/hooks/useApi.ts`

| 훅 | 변경 |
|---|---|
| `useOnboardingStep2Questions` | `enabled` 파라미터 추가, `retry: false` |
| `useAvailableCourses` | 반환 타입 `AvailableCoursesResponse` → `AvailableCourse[]` |
| `useCourseGenerateStatus` | **신규** — `GET /course/generate/status/:purchaseId` 10초 폴링 |
| 전체 폴링 훅 | 3초 → **10초** 간격으로 변경 (3곳) |

---

## 7. 타입 추가

**파일:** `src/types/course.ts`

```typescript
// 마이북 생성 진행률 (서버 폴링 응답)
interface GenerationProgress {
  step: string;           // "챕터 2/5 생성 완료"
  percent: number;        // 0~100
  chaptersDone: number;
  totalChapters: number;
  updatedAt: string;
}

// OnboardingStep4StatusResponse에 progress 필드 추가
// CourseGenerateStatusResponse 신규 타입
```

---

## 8. 백엔드 변경사항 (별도 커밋 필요)

**경로:** `/Users/sieun/Documents/moneyrun/backend`

| 파일 | 변경 |
|---|---|
| `onboarding.service.ts` | step1: `.single()` → `.maybeSingle()` + upsert 에러 체크 |
| `my-book.service.ts` | user_courses 쿼리에 `status` 컬럼 추가 + active 우선 탐색 |
| `course-book.generator.ts` | 생성 progress 업데이트 로직 (백엔드 세션에서 작업) |
| `course.service.ts` | `GET /course/generate/status/:purchaseId` 엔드포인트 추가 |
| `course.controller.ts` | 생성 상태 폴링 라우트 추가 |

---

## 이어서 할 작업

1. 백엔드 변경사항 커밋 + 푸시 (별도 진행)
2. `diagnostic_quizzes` 시드 데이터 확인 — 5개 카테고리 × 10문제 존재 여부
3. 실제 온보딩 전체 플로우 E2E 테스트 (step1~5 → 페이스메이커)
4. 코스 완료 → 다음 코스 시작 → 마이북 생성 → 완료 플로우 테스트

---

*변경사항 문서 — 2026.04.16*
